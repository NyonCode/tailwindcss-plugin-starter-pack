const plugin = require('tailwindcss/plugin')

module.exports = plugin(
  function ({}) {
    // Plugin implement
  },
  {
    theme: {
      extend: {
        // Theme extension
      },
    },
  }
)
